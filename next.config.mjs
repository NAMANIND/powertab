/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.builder.io",
      "firebasestorage.googleapis.com",
      "www.theiecna.com",
    ],
  },
};

export default nextConfig;
